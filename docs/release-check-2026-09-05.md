# 2026-09-05 发布审查

本轮检查个人同步、任务历史、小组权限与页面交互，以及桌面和 Android 构建。修复两处界面竞态：切换小组会清除旧小组的解散/移除确认框；修改邀请码或关闭申请表单后，迟到的查询响应不会恢复旧的小组预览。回归测试加入 `npm run test:groups`。

验证结果：

- `npm run verify` 通过，包括 37 项 Rust 测试。最终前端修改后重新通过 `test:unit`、`test:groups` 和各平台构建。
- 17 项浏览器检查通过：两用户创建/申请/审批、默认不公开、备注隐藏、撤销分享、退出、账号切换、断网和迟到响应处理；1100/680/390 像素宽度无横向溢出。浏览器使用隔离数据库测试数据。
- Windows 便携版启动并响应，原生 WebView 中存在“我的小组”入口；发布前保留本地数据备份。
- Windows NSIS 安装程序构建成功；Android ARM64 debug APK 构建成功、v2 签名验证通过。APK 内 JS/CSS 与本次 dist 逐项 SHA-256 一致，桌面程序内包含相同资源文件名。
- npm 官方源安全扫描 0 个已知漏洞；源码与待推送历史扫描未发现配置密钥或私钥签名，桌面便携包路径隐私扫描通过。

验证边界：未连接 Android 真机，因此未验证实际安装、系统后台恢复或手机通知；NSIS 已构建但未执行安装向导；当前未安装 cargo-audit，Rust 依赖漏洞审计未完成。浏览器模拟不替代多台真实设备长时间同步及大规模小组性能测试。任务历史仍以云端接收状态为准，离线合并后的中间变化不能重建。

产物保留在本地 `release/`，不提交 Git：

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| 小光任务.exe | 11113984 | DEAD0D1F6B78D06FD3AECF4EDB8CCC939A35FF0A12FA3E0200BC450AFBF4A278 |
| 小光任务_0.2.1_x64-setup.exe | 4050890 | D86F0A176718C2007DE5B448F58D2D6F183B4A0F3BE16D1CF0307919A5EE52BA |
| 小光任务-android-arm64-debug.apk | 131619626 | D886F663049748E36A9D158B53F97A6DF787F75A49855C8D88A46AC35BBD2D80 |

前端资源：`app-DLYvRlt0.js`、`app-qamPd4Nn.css`、`dist-BvBIV9Vl.js`。完整本地路径与生成时间见 `release/verification-2026-09-05.json`。

## 11:01 移动端界面更新

根据手机截图压缩首页标题、日期、进度和状态筛选区域，任务行调整为紧凑布局；侧栏底部添加导出、导入、设置文字。日期筛选统一从筛选面板操作。导航栏总高度包含状态栏安全区，菜单和主题按钮保留 44px 点击区域；抽屉底部避开系统手势区。

单元测试、小组测试、前端与 Android 构建通过。浏览器验证 320/390/430 像素宽度、横屏、深色主题、30px 顶部安全区，及搜索、筛选、完成、中文输入、新增任务、焦点恢复。未连接真机，系统安全区最终效果仍需实际设备确认。

本轮仅替换 Android APK，桌面产物哈希保持上表不变。新 APK：131619266 字节，生成时间 2026-09-05 11:01:16 +08:00，SHA-256 为 `6225B754897A90E0013F180870A6DC7ED397579601ECA6A532DB9C458E31CBF8`。签名校验通过，包内 `app-f8uWFjUG.js`、`app-CC3qrQNG.css`、`dist-BvBIV9Vl.js` 与本轮构建逐项哈希一致。完整路径见 `release/mobile-ui-verification-2026-09-05.json`。

## 16:13 焦点修复与再次审查

取消项目挂载时自动聚焦添加任务输入框，避免切换项目后亮起黄边；添加和搜索输入使用容器的单层焦点提示，保留 Ctrl+N、Ctrl+F 和添加子任务的主动聚焦。浏览器验证首次加载、连续切换三个项目、快捷键焦点与边框；另通过 23 项桌面交互/布局检查，覆盖搜索、筛选、任务编辑、中文输入与 680–1600px 宽度。

全套 verify（含 37 项 Rust 测试）通过。后续新增数据库加固测试也通过，npm 官方源扫描为零已知漏洞。迁移 `20260905081031_harden_helper_and_group_indexes.sql` 已在线应用并读回验证：撤销系统事件辅助函数不必要的 anon/authenticated 执行权限、保留原事件触发器、补充三个外键索引。新测试同时覆盖辅助函数缺失与存在两种环境。剩余免费套餐限制及容量模型见 [云端容量说明](cloud-capacity.md)。

以下为本日最新产物，已覆盖同名文件；桌面启动且响应正常，APK 签名与资源哈希验证通过。桌面包含 `app-DyTNTEqy.js`、`app-DkXIWch3.css`、`dist-BvBIV9Vl.js`，APK 对应资源与 dist 逐项一致。依然没有执行 NSIS 安装向导或 Android 真机安装；此前关于 Rust 依赖漏洞审计及真实设备长期同步的验证边界不变。

| 文件 | 字节数 | 生成时间（+08:00） | SHA-256 |
| --- | ---: | --- | --- |
| 小光任务.exe | 11113984 | 16:11:32 | C228AFC13D9BB342C34F44A8D8023386C5E671D1FF00F0A212BBA67AF88AB73D |
| 小光任务_0.2.1_x64-setup.exe | 4049855 | 16:13:14 | 4C5E080ED8436A18F4747C518B83082CCDAF969FB84301D8D547D595A971A7B8 |
| 小光任务-android-arm64-debug.apk | 131619234 | 16:12:12 | 90A544EAFBE1280C03FAD6DAE6E07047D1A075556EED07719B97F36656AFE6F6 |

完整路径与哈希还保存在本地 `release/focus-fix-verification-2026-09-05.json`。
