# TaskFlow 启动说明

## 环境要求
- Node.js 18+ （https://nodejs.org 下载）
- Rust / Cargo（https://www.rust-lang.org/tools/install ，开发和构建需要）

## 第一次启动（开发模式）

打开终端，进入 TaskFlow 目录：

```bash
cd TaskFlow

# 安装依赖（仅需执行一次）
npm install

# 启动开发模式
npm.cmd run dev
```

等待约 10 秒，应用窗口会自动弹出。

---

## 当前正式版（轻量免安装版）

当前保留的正式版位于：

`release/小光任务.exe`

双击即可运行。

---

## 重新构建当前正式版

```bash
npm.cmd run build
```

构建脚本会自动把 `src-tauri/target/release/小光任务.exe` 复制到：

`release/小光任务.exe`

> 说明：项目已完全迁移到 Tauri，Electron 版已于 2026-06-11 移除（git 历史中可找回）。

---

## 功能说明

| 操作 | 方式 |
|------|------|
| 添加任务 | 顶部输入框，回车确认 |
| 设置截止日期 | 添加任务时选择日期，或悬停任务后用日期控件修改 |
| 打开任务详情 | 单击任务标题 |
| 编辑备注 / 优先级 / 标签 | 打开任务详情面板后编辑 |
| 设置重复任务 | 任务详情面板中选择每天 / 每周 / 每月 |
| 到期提醒 | 应用启动后自动提醒今天截止和已逾期任务 |
| 完成任务 | 点击左侧复选框 |
| 编辑任务标题 | 双击任务文字 |
| 添加子任务 | 鼠标悬停，点击 + 按钮，输入框变为子任务模式 |
| 删除任务 | 鼠标悬停，点击 × 按钮 |
| 拖动排序 | 拖拽任务行上下移动 |
| 搜索 / 筛选 | 任务列表顶部搜索框、状态分段控件、日期筛选 |
| 智能视图 | 左侧「今天」「近 7 天」「已完成」快速查看 |
| 新建项目 | 左侧栏底部「新建项目」按钮 |
| 重命名项目 | 右键项目名 → 重命名 |
| 删除项目 | 右键项目名 → 删除项目 |
| 备份数据 | 左下角导出/导入按钮，或进入设置页操作 |
| 切换主题 | 设置页「主题配色」选择晨雾 / 墨蓝 / 森林 / 石墨 / 暮杏 |
| 桌面组件 | 设置页「桌面组件」选择项目，并点击「显示组件」 |
| 托盘后台运行 | 点击主窗口关闭按钮后，程序隐藏到右下角托盘 |
| 托盘菜单 | 右键托盘图标，可显示主窗口、显示/隐藏桌面组件、退出 |
| 导出诊断日志 | 设置页「诊断日志」区域导出 |
| 清空诊断日志 | 设置页「诊断日志」区域清空 |

## 快捷键

| 快捷键 | 操作 |
|--------|------|
| Ctrl + F | 聚焦搜索 |
| Ctrl + N | 聚焦新增任务 |
| Ctrl + 1 | 打开「今天」 |
| Ctrl + 2 | 打开「近 7 天」 |
| Ctrl + 3 | 打开「已完成」 |
| Ctrl + , | 打开设置 |
| Esc | 关闭确认框或任务详情 |

## 验证命令

```bash
# 发版前基础回归：渲染端构建 + Rust cargo check
npm.cmd run verify

# 生成 Windows 图标
npm.cmd run generate:icon

# 渲染端构建
npm.cmd run build:renderer

# 生成当前 Tauri 轻量版 exe
npm.cmd run build
```

## 数据存储位置

数据自动保存在应用数据目录，重装应用通常不会丢失：

`%APPDATA%\com.personal.taskflow-lite\taskflow-data.json`

（实际路径可在设置页「应用信息」中查看。）

## 自动备份

应用启动时会自动创建一份备份，导入外部备份前也会自动创建一份备份。

默认保留最近 30 份：

`%APPDATA%\com.personal.taskflow-lite\backups`

## 数据版本与诊断

应用数据包含 `schemaVersion` 字段，旧数据会在读取时自动补齐新字段。

设置页会显示日志路径和最近诊断日志，便于排查导入、启动和数据恢复问题。

## 发布说明

- 应用图标资源位于 `assets/icon.svg` 和 `assets/icon.ico`。
- `assets/icon.ico` 可通过 `npm.cmd run generate:icon` 重新生成。
- 当前 Windows zip 便携包关闭了 exe 签名编辑流程，以便在普通权限环境稳定打包。
- 正式公开分发前建议配置代码签名证书。


## TaskFlow Lite（Tauri 轻量版迁移）

当前项目已开始迁移到 Tauri，以降低运行内存。迁移说明见：

`TAURI_MIGRATION.md`

安装 Rust / Tauri 环境后可使用：

```bash
npm.cmd run lite:check
npm.cmd run lite:exe
npm.cmd run lite:dev
npm.cmd run lite:build
```

`lite:exe` 会生成免安装轻量版：

`release/小光任务.exe`

当前实测 Lite 版启动后工作集约 33.7 MB，私有内存约 11.2 MB。`lite:build` 会继续尝试生成安装包，但可能因为 NSIS 打包器下载超时失败；日常轻量使用优先看 `lite:exe`。

## 桌面组件与托盘验收

1. 打开 `release/小光任务.exe`。
2. 进入设置页，找到「桌面组件」。
3. 选择要显示的项目，点击「显示组件」。
4. 桌面组件可以拖动位置、调整大小、切换置顶、切换紧凑模式。
5. 点击主窗口右上角关闭按钮，主窗口会隐藏到系统托盘，程序继续后台运行。
6. 右键托盘图标，可以重新显示主窗口、显示/隐藏桌面组件，或退出程序。
