# TaskFlow domain vocabulary

这份词汇表只定义产品概念，避免本地桌面、移动端和云端实现使用不同含义。

- **工作区（Workspace）**：用户可见的完整任务数据集合，包含项目与任务。
- **设备（Device）**：一个安装或登录工作区的客户端实例，用于标识本地变更来源。
- **本地变更（Local change）**：用户在当前设备上创建、修改、完成、删除或排序任务/项目的动作。
- **待同步队列（Outbox）**：本地变更在获得云端确认前的持久化队列；断网时仍可继续操作。
- **同步游标（Sync cursor）**：客户端已确认接收的服务端变更位置，用于增量拉取。
- **软删除标记（Tombstone）**：用于让删除动作可同步、可重放并避免另一设备恢复旧数据的记录。
- **冲突（Conflict）**：两个设备基于不同版本同时修改同一实体，需要按产品规则合并或请求用户选择。
