# TaskFlow（小光任务）项目评估报告

日期：2026-06-11

## 一、项目概况

- 定位：轻量个人任务管理器（Windows 桌面端），Vue 3 + Vite 前端，双后端并存：Electron（旧）+ Tauri 1.x（新，主力）。
- 代码量：约 7000 行。核心文件：`src-tauri/src/main.rs`（1775 行）、`src/App.vue`（1458 行）、`src/shared/taskflow-core.cjs`（286 行）。
- 磁盘占用：2.6 GB，其中 `src-tauri/target/` 2.1 GB、`node_modules/` 519 MB —— 均为可重建的构建产物。
- 整体评价：**质量良好**。数据层有原子写入（tmp→rename + 重试）、自动备份（启动/迁移/导入前，保留 30 份）、损坏自恢复、日志轮转、schema 版本迁移；Electron 端 contextIsolation 开启；Rust 代码零 `unwrap()`。Tauri 迁移已基本完成，内存从 ~500 MB 降到 ~20-35 MB，效果显著。

## 二、发现的问题

### 🔴 高优先级

1. **时区 Bug：「今天」按 UTC 计算**
   三处均用 UTC 取日期：`App.vue:274` `new Date().toISOString().slice(0,10)`、`taskflow-core.cjs:146`、`main.rs` `Utc::now().date_naive()`。
   北京时间（UTC+8）每天 0:00–8:00 之间，应用认为"今天"是昨天：今日视图不显示当天任务、到期提醒漏报/错报、重复任务跳期。
   修复：JS 端改用本地日期拼接（`getFullYear/getMonth/getDate`），Rust 端改用 `chrono::Local::now().date_naive()`。

2. **无版本控制**
   项目没有 git 仓库。修复：`git init` + `.gitignore`（排除 `node_modules/`、`src-tauri/target/`、`dist/`、`release/`、`备份/`）。这是成本最低、收益最高的一项。

### 🟡 中优先级

3. **业务逻辑双份维护，已出现漂移**
   同一套规则在 Rust（main.rs）和 JS（taskflow-core.cjs）各实现一份。已有分歧实例：默认示例任务 Rust 2 条 / JS 3 条；备份保留份数代码 30 份 / 文档写 12 份。Electron 版作为"回退方案"持续产生维护税（electron 占 node_modules 大头）。
   建议：确认 Tauri 版稳定后，移除 Electron 路径（main.js/store.js/preload.js、electron 相关依赖与脚本），单一事实来源收敛到 Rust。

4. **每次操作全量读写 JSON，且同步命令跑在主线程**
   Tauri 1.x 同步 command 在主线程执行；每个增删改都是 读整文件→解析→改→pretty 序列化→写整文件。个人规模（几百条任务）没问题，但任务量增长后会出现 UI 卡顿。
   建议：用 `tauri::State<Mutex<TaskFlowData>>` 内存态 + 防抖落盘（如 500ms），或至少给命令加 `#[tauri::command(async)]`。

5. **Rust 核心零测试**
   `test:smoke` 只测 Electron store（即将退役的那条路径）；main.rs 里的 normalize、迁移、重复任务、备份恢复逻辑无任何测试。
   建议：为 `normalize_stored_data`、`next_repeat_date`、`normalize_import_data`、`collect_task_tree` 写 `cargo test` 单元测试，纳入 `verify` 脚本。

### 🟢 低优先级

6. **安全配置可收紧**：`tauri.conf.json` 中 `csp: null`、`window.all: true`、`dialog.all: true`。本地内容风险不大，但建议设置基本 CSP、按需列出 window 权限。
7. **版本号不一致**：package.json `1.0.0` vs tauri.conf `0.1.0`。
8. **文档漂移**：启动说明写"保留 12 份备份"，代码实际 30 份；数据路径说明仍以 Electron 路径为主，Tauri 版实际路径不同。
9. **App.vue 过大**（1458 行）：建议拆出 `useTasks`/`useProjects`/`useSettings` composables，设置页拆为独立组件。
10. **Tauri 1.x 已落后**：Tauri 2 已稳定，1.x 维护放缓。非紧迫，但新功能（如移动端、更细粒度权限）只在 2.x。
11. **磁盘清理**：`cargo clean`（省 2.1 GB）；若弃用 Electron，删 electron 依赖后 `node_modules` 可大幅缩小。

## 三、建议执行顺序

| 顺序 | 事项 | 工作量 | 收益 |
|---|---|---|---|
| 1 | git init + .gitignore | 10 分钟 | 防丢码，后续一切改动可回滚 |
| 2 | 修时区 Bug（3 处） | 1 小时 | 修正核心功能正确性 |
| 3 | Rust 单元测试覆盖数据层 | 半天 | 改动安全网 |
| 4 | 下线 Electron 路径 | 半天 | 消除双维护、瘦身依赖 |
| 5 | 内存态 + 防抖落盘 | 1 天 | 性能上限、消除主线程 IO |
| 6 | App.vue 拆分、收紧 CSP、统一版本号与文档 | 渐进 | 可维护性 |
